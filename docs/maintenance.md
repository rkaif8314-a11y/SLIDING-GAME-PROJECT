# Sliding Game Maintenance

Keep game state transitions deterministic and ensure user input cannot leave the board in an impossible state. Preserve keyboard and touch usability when changing controls.
